[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "piqrypt-autoGen-integration"
version = "1.1.0"
description = "PiQrypt cryptographic audit trail bridge for Microsoft AutoGen"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "PiQrypt Contributors", email = "contact@piqrypt.com"}
]
requires-python = ">=3.9"
keywords = ["cryptography", "autogen", "microsoft", "audit", "ai-agents", "aiss", "multi-agent"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Security :: Cryptography",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "piqrypt>=1.8.6",
    "pyautogen>=0.2.0",
]

[project.urls]
Homepage = "https://github.com/piqrypt/piqrypt-autoGen-integration"
Documentation = "https://github.com/piqrypt/piqrypt/blob/main/INTEGRATION.md#4-autogen"
Repository = "https://github.com/piqrypt/piqrypt-autoGen-integration"
"Bug Tracker" = "https://github.com/piqrypt/piqrypt/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["piqrypt_autogen*"]
